# Production build (local test of combined API + static client)
NODE_ENV=production
PORT=3001
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/team_task_manager
CORS_ORIGIN=http://localhost:3001
JWT_SECRET=local-production-test-secret-minimum-32-characters-long
SERVE_CLIENT=true
# VITE_API_URL=   (leave unset for same-origin)
